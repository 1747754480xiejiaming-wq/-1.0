# 三代开发 Bug 追踪规则

开发问题按 `FE-YYYY-NNN`、`BE-YYYY-NNN`、`INT-YYYY-NNN` 和 `DEV-ENV-NNN` 编号，分别表示前端、后端、联调和开发环境问题。

每条记录至少包含：发现版本、状态、严重度、复现步骤、预期结果、实际结果、影响范围、修复提交和回归结果。状态使用 `新建`、`已复现`、`修复中`、`待回归`、`已关闭`；无法复现时保留证据，不直接删除。

提交修复时在提交信息中引用 Bug ID。测试阶段重新出现的问题沿用原 Bug ID，并增加复现版本和回归记录。

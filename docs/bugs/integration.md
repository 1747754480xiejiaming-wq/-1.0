# 联调 Bug 台账

| Bug ID | 发现版本 | 状态 | 严重度 | 摘要 | 复现与证据 | 处理与回归 |
| --- | --- | --- | --- | --- | --- | --- |
| INT-2026-001 | `be-v3.0.0-dev.2` 同步至前端 | 待回归 | 中 | OpenAPI 原始字节哈希受 LF/CRLF 转换影响 | 后端生成哈希 `d9916a...`；前端检出同一快照后计算为 `093cb4...`，`api:check` 和契约测试失败 | `dev.2.1` 改为统一换行后计算哈希；须在前后端两个 Worktree 回归 |

新增问题使用 `INT-YYYY-NNN` 编号，并按照 [追踪规则](README.md) 记录请求、响应、关联 ID、前后端版本和回归结果。

# 联调 Bug 台账

| Bug ID | 发现版本 | 状态 | 严重度 | 摘要 | 复现与证据 | 处理与回归 |
| --- | --- | --- | --- | --- | --- | --- |
| INT-2026-001 | `be-v3.0.0-dev.2` 同步至前端 | 已关闭 | 中 | OpenAPI 和生成文件比较受 LF/CRLF 转换影响 | `dev.2` 在前端同时出现哈希与全文比较失败；`dev.2.1` 修复哈希后，6/6 契约测试通过但 `api:check` 仍因生成文件全文换行差异失败 | `dev.2.2` 同时规范化哈希输入和全文比较；后端 98/98 全量测试及构建通过，前端 CRLF 工作区 `api:check`、6/6 契约测试、类型检查和构建通过 |

新增问题使用 `INT-YYYY-NNN` 编号，并按照 [追踪规则](README.md) 记录请求、响应、关联 ID、前后端版本和回归结果。

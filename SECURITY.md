# 安全说明

请勿提交 `.env`、`data/`、`.local/`、QQ 凭证、模型密钥、SQLite 数据库、运行日志、测试报告或桌面运行时二进制。仓库的 `.gitignore` 已覆盖这些常见路径，但提交前仍应检查 `git status`。

首次执行 `npm start` 或 `npm run setup` 时，系统会为当前安装生成独立的运行时密钥，并保存在本机忽略目录中。不要把这些信息复制到 Issue、日志、截图或公开仓库。

如发现安全问题，请通过仓库维护者提供的私密渠道报告，不要在公开 Issue 中附带可用凭证、学生数据或原始消息内容。
